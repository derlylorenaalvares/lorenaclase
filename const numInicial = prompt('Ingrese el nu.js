const numInicial = prompt('Ingrese el numero inicial:');
const numEval = prompt('Ingrese el numero de elementos del array:');
let nums = [];
for(let i = 1; i <= numEval; i++) {
    const randomNum = Math.floor(Math.random() * 100) + 1;
    nums.push(randomNum);
}
const edad = prompt('ingrese la primera edad')
if ( edad >=0; 12 )

    let canasta =[" manzana,"]

    canasta.foreach (IIRFilterNode, key)=> [ console.log ('item=?') 