/*const frutas = array('mango', 'fresa', 'banano')
console.log(frutas)
console.log(frutas.length)


const justOneNumber = Array(12)
console.log(justOneNumber)

const oneNumber = [5, 'hola']
console.log(OneNumber)

const resipeIngredients = [
'fruta',
true,
2,
{
    ingredient: 'leche',quality: '1 cup'
},
false

]
console.log(resipeIngredients)

recipeIngrediens.push('frutas')
console.log(recipeIngrediens)

const newfruits = frutas.concat(['frutas', 'verduras'])
console.log(newfruits)

const frutas = Array('kiwi', 'naranja' , 'manzana')
console.log(frutas)
console.log(frutas.length)

const justOneNumber = Array(12)
console.log(justOneNumber)

const OneNumber = [4, 'estamos bien']
console.log(OneNumber)

const recipeIngredients = [
    'tazon',
    true,
    2,
    {
        ingredient: 'milk', quality: '1 cup'
    },
    false

]*/
/*
const frutas = Array('kiwi', 'naranja' , 'manzana')
console.log(frutas)
console.log(frutas.length)

const justOneNumber = Array(12)
console.log(justOneNumber)

const OneNumber = [4, 'estamos bien']
console.log(OneNumber)

const recipeIngredients = [
    'tazon',
    true,
    2,
    {
        ingredient: 'milk', quality: '1 cup'
    },
    false

]
console.log(recipeIngredients)

recipeIngredients.push('menos')
console.log(recipeIngredients)

const newFruits = frutas.concat(['kiwi', 'mango'])
console.log(newFruits)

const isArray = Array.isArray(frutas)
console.log(Array.isArray(frutas))

const array= [ 1,2,3,4,4];
const mapped =array.map((x) => x *
2);
console.log(mapped)

const filtrado = rosa.filter((nombre) => number > 3);
console.log(filtrado);


const cadenas = [ "casa", "pueblo","pais"];
console.log( cadenas.slise(3));
array.pop();
console.log(array);

cadenas.sort();
console.log(cadenas);

const islargeNumber =((nombre) => number > 10);
console.log( )
*/
/* 
const array = [15, 11, 25, 13];
 const mapped = array.map ((x) => x * 3);

 console.log(mapped);

 const filtrado = array.filter((number) => number > 3);
 console.log(filtrado);

 const cadenas = ["dinero", "money", "ropa", "casas"]
 console.log(cadenas.slice(3));
 
 array.pop();
 console.log(array);

 cadenas.sort();
 console.log(cadenas);

 function compareNumbers( a,b){
    return a - b;

 }

 array.sort(compareNumbers);
 console.log(array);


 const isLargeNumber = (number) => number > 7;
 console.log(array.findIndex(isLargeNumber));


 const carrito =[
 {producto:'mouse',valor:50000}
 {producto: 'teclado', valor:50000}
 {producto: 'monitor',valor:50000} 
 ]
 const total =carrito.reduce ((acom.item) => acom + item.valor, 0);
*/
/*
 const mensaje= ["hola","pasto","colombia","pais"];

 const worfrecuency =mensaje.reduce((acumulador,currenteValue)  {
    if (acomulador [currenteValue])++
        acumulador[currentiValue]
    } else {
        accumulator[ currenteValue] = 1
    }
    return accumulator
}
*/
/*
class animal {
    construtor( nombre ,tipo) {
    this.nombre=nombre;
    this.tipo=tipo;
    }
emitirsonido() {
    console .log("el animal emite un sonido");
    }
}

  class perro extends animal {
    construtor( nombre, tipo,raza) {
    super(nombre, tipo);
   this.raza = raza;
   }
     emitirsonido(){
    console.log("el perro ladra")
}
    Corer() {
   console.log(`${this.nombre}corre alegre`);
    }
}
const perro1=new perro("babby","perra","pug");
 console.log(perrol);
 perro1.correr();
 perro1.emitirsonido();

 perro.nuevometodo = function (){
    console.log("este es un metodo");
 };

 perro1.prototype.segundometodo = function(){
    console.log("es un nuevo metodo");
 };


 class persona{
    conatrutor( nombre,edad){
    this.nombre=nombre;
    this.edad=edad;
    }
 }
 const persona1 = new persona ("alice,25");

 comsole.log(persona);
 persona1.nuevometodo = function (){
    console.log ("mi nimbre es ${this.nombre}");
 };

 const userdatabase = 1 
 username : "buscar";
 paddword:"bebe loper es lo mejor";

 const  = 1
 usurname : "buscar"
timeline:"bebe loper es lo mejor";
const userstimelime
*//*
Requerimientos del reto:
3
​
4/*
1. El usuario debe poder ingresar su usuario y contraseÃ±a
5
2. El sistema debe ser capaz de validar si el usuario y contraseÃ±a ingresados por el usuario existen en la base de datos
6
3. Si el usuario y contraseÃ±a son correctos, el sistema debe mostrar un mensaje de bienvenida y mostrar el timeline del usuario.
7
4. Si el usuario y contraseÃ±a son incorrectos, el sistema debe mostrar un mensaje de error y no mostrar ningun timeline.
8
​
9
*/








